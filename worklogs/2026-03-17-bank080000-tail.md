# 2026-03-17 bank080000 tail pass

- Split the `0x098000-0x09F776` non-fill island out of the coarse tail-bank `incbin` into
  `src/bank080000_mid.asm` so the bank now has ROM-order source boundaries inside its first opaque
  post-fill slice.
- Kept the front `0x098000-0x09991F` region conservative but no longer anonymous: the byte pattern is
  strongly opcode-dense and Z80-like, so it now has an explicit structural label instead of hiding
  inside `Bank080000_MidData_098000_09F776`.
- Split the following structurally distinct records without over-claiming subsystem ownership:
  `0x099920-0x09997F` descriptor-header records, `0x099980-0x099A84` packed layout/coordinate data,
  `0x099A85-0x099AFF` zero fill, and `0x099B00-0x09F776` pointer-and-command data.
- The `0x099B00+` region begins with what looks like a little-endian pointer table into later
  command-like records; the first obvious step after this pass is to find the 68k-side loader or
  bank-switch path that consumes those tables so the structural labels can be promoted into real
  subsystem names.
- Follow-up raw-byte work tightened the front of that same command region: `0x099B00-0x099B31` now
  stands as a short command lead-in, while `0x099B32-0x099BAF` is explicit in source as a big-endian
  bank-local command offset table. Almost every entry lands back inside the surrounding
  `0x099B00-0x0A0348` payload, with one notable outlier word (`$F7FD`) still left literal until a
  loader or decoder proves what it means.
- Split the next tail-bank non-fill island at `0x0A0000-0x0A4C76` out of the coarse owner into
  `src/bank080000_a0000.asm`.
- The front `0x0A0000-0x0A07C5` bytes are now source-authored as a nested big-endian offset-table
  tree instead of one anonymous blob: a root table at `0x0A0000`, a dense second-stage table block
  at `0x0A01C8`, and a short tail at `0x0A07BC`.
- Most of those words point back into the same island, either to later table entries or into the
  unresolved payload at `0x0A07C6+`, which is enough evidence to treat the front slice as structured
  table data even though the subsystem owner and downstream record format are still open.
- Split the first payload band at `0x0A07C6-0x0A08E5` out of the remaining `0x0A07C6+` tail into its
  own source module, `src/bank080000_a07c6.asm`, instead of leaving the whole payload behind one
  trailing incbin.
- The new module is still conservative but no longer opaque: it shows twelve repeated `0x18`-byte
  records in ROM order, each made of four 6-byte tuples. The tuple meaning is still unresolved, so
  the labels stay structural until a consumer or decoder proves more.
- Follow-up inspection of the downstream `0x0A08E6+` tail did not justify another fine-grained source
  split yet, but it did sharpen the format hypothesis: the payload still parses coherently as a much
  longer 6-byte tuple stream, with a more homogeneous motif around `0x0A0E86`. That looks like a
  useful future boundary candidate, but not a loader-proven ownership split yet.
- A later pass found a stronger ROM-order split for the back half of the same island without needing
  new semantic guesses: the front offset-table tree already references stable starts throughout
  `0x0A3500-0x0A4C76`, so that span now lives in `src/bank080000_a3500.asm` as table-targeted record
  slices instead of staying inside one trailing `incbin`.
- Kept `0x0A08E6-0x0A34FF` separate as an unresolved lead-in. The bytes there still fit the earlier
  long 6-byte-tuple hypothesis, but the table evidence becomes much stronger at `0x0A3500`, making
  that the safer next decomposition point for visible progress.
