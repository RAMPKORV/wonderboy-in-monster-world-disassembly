# 2026-03-17 bank080000 tail pass

- Split the `0x098000-0x09F776` non-fill island out of the coarse tail-bank `incbin` into
  `src/bank080000_z80_resources.asm` so the bank now has ROM-order source boundaries inside its first opaque
  post-fill slice.
- Kept the front `0x098000-0x09991F` region conservative but no longer anonymous: the byte pattern is
  strongly opcode-dense and Z80-like, so it now has an explicit structural label instead of hiding
  inside `Bank080000_MidData_098000_09F776`.
- Split the following structurally distinct records without over-claiming subsystem ownership:
  `0x099920-0x09997F` descriptor-header records, `0x099980-0x099A84` packed layout/coordinate data,
  `0x099A85-0x099AFF` zero fill, and `0x099B00-0x09F776` pointer-and-command data.
- The `0x099B00+` region begins with what looks like a little-endian pointer table into later
  command-like records; the first obvious step after this pass is to find the 68k-side loader or
  bank-switch path that consumes those tables so the structural labels can be promoted into real
  subsystem names.
- Follow-up raw-byte work tightened the front of that same command region: `0x099B00-0x099B31` now
  stands as a short command lead-in, while `0x099B32-0x099BAF` is explicit in source as a big-endian
  bank-local command offset table. Almost every entry lands back inside the surrounding
  `0x099B00-0x0A0348` payload, with one notable outlier word (`$F7FD`) still left literal until a
  loader or decoder proves what it means.
- Split the next tail-bank non-fill island at `0x0A0000-0x0A4C76` out of the coarse owner into
  `src/bank080000_offset_tree_payload.asm`.
- The front `0x0A0000-0x0A07C5` bytes are now source-authored as a nested big-endian offset-table
  tree instead of one anonymous blob: a root table at `0x0A0000`, a dense second-stage table block
  at `0x0A01C8`, and a short tail at `0x0A07BC`.
- Most of those words point back into the same island, either to later table entries or into the
  unresolved payload at `0x0A07C6+`, which is enough evidence to treat the front slice as structured
  table data even though the subsystem owner and downstream record format are still open.
- Split the first payload band at `0x0A07C6-0x0A08E5` out of the remaining `0x0A07C6+` tail into its
  own source module, `src/bank080000_a07c6.asm`, instead of leaving the whole payload behind one
  trailing incbin.
- The new module is still conservative but no longer opaque: it shows twelve repeated `0x18`-byte
  records in ROM order, each made of four 6-byte tuples. The tuple meaning is still unresolved, so
  the labels stay structural until a consumer or decoder proves more.
- Follow-up inspection of the downstream `0x0A08E6+` tail did not justify another fine-grained source
  split yet, but it did sharpen the format hypothesis: the payload still parses coherently as a much
  longer 6-byte tuple stream, with a more homogeneous motif around `0x0A0E86`. That looks like a
  useful future boundary candidate, but not a loader-proven ownership split yet.
- A later pass found a stronger ROM-order split for the back half of the same island without needing
  new semantic guesses: the front offset-table tree already references stable starts throughout
  `0x0A3500-0x0A4C76`, so that span now lives in `src/bank080000_table_targeted_payload_records.asm` as table-targeted record
  slices instead of staying inside one trailing `incbin`.
- Kept `0x0A08E6-0x0A34FF` separate as an unresolved lead-in. The bytes there still fit the earlier
  long 6-byte-tuple hypothesis, but the table evidence becomes much stronger at `0x0A3500`, making
  that the safer next decomposition point for visible progress.
- The next pass tightened that front lead-in too: `0x0A08E6-0x0A0B25` now lives in
  `src/bank080000_a08e6.asm` as twenty-four more ROM-order `0x18`-byte records instead of one
  anonymous lead-in blob.
- This new slice stays structural rather than semantic. The cadence is the proof: it continues the
  same four-6-byte-tuple layout already source-authored at `0x0A07C6-0x0A08E5`, while the bytes only
  stop cleanly fitting that family at the new unresolved boundary `0x0A0B26`.
- Raw-byte review also sharpened the next likely follow-up without overcommitting: a later
  `0xFF`-delimited record family around `0x0A0E85` now looks like the next conservative split
  candidate once it is worth carving the remaining middle patch more finely.
- Followed through on that next split: `src/bank080000_offset_tree_payload.asm` now peels four narrower record bands
  out of the old monolithic `0x0A0B26-0x0A34FF` lead-in. The new `src/bank080000_a0c83.asm` module owns
  FF-terminated 6-byte runs at `0x0A0C83-0x0A0D18`, `0x0A0D3F-0x0A0D74`, `0x0A0E85-0x0A0F2C`, and
  `0x0A0FA9-0x0A10AA`, while the bytes between them remain explicit mixed-payload gaps instead of being
  flattened back into one anonymous incbin.
- The evidence for those four bands is local but solid: each record is exactly 6 bytes, the sixth byte
  is always `$FF`, and the surrounding values cluster into repeated families such as `$0A/$05/$0B/$0E`
  in byte 0 and `$00/$20/$60/$02` in byte 1, with signed offset-like tails (`$F4/$F8/$FC`) in bytes 3-4.
  That is enough for a mature structural split, but not yet enough to force a sprite/object/credits/map
  interpretation without the loader.
- A further pass broke the remaining `0x0A10AB-0x0A34FF` middle tail into a much cleaner ROM-order
  module, `src/bank080000_a10ab.asm`, instead of leaving that whole span behind one last monolithic
  `incbin`.
- The strongest new evidence there is delimiter cadence rather than subsystem meaning: most records in
  that region end in `$FF`, and the bytes before each terminator fall cleanly into 5-byte units. That
  exposes long structural runs at `0x0A10AB-0x0A1321`, `0x0A132D-0x0A136D`, `0x0A1378-0x0A1405`,
  `0x0A1411-0x0A1D3E`, `0x0A1D4F-0x0A23F5`, `0x0A2400-0x0A2585`, `0x0A258B-0x0A2605`,
  `0x0A2610-0x0A2764`, `0x0A27CB-0x0A2D85`, `0x0A2D9B-0x0A303F`, and `0x0A304B-0x0A34FF`.
- Short anomaly gaps now stand explicit between those runs at `0x0A1322-0x0A132C`, `0x0A136E-0x0A1377`,
  `0x0A1406-0x0A1410`, `0x0A1D3F-0x0A1D4E`, `0x0A23F6-0x0A23FF`, `0x0A2586-0x0A258A`,
  `0x0A2606-0x0A260F`, `0x0A2765-0x0A27CA`, `0x0A2D86-0x0A2D9A`, and `0x0A3040-0x0A304A`, which is a far
  better human-readable stopping point than one anonymous `0x0A10AB+` remainder.
- Kept the names structural. The new split proves FF-delimited 5-byte tuple cadence, but it still does
  not prove whether the tuples are layout cells, object entries, script atoms, or another format without
  the code path that consumes the surrounding offset-table tree.
- Pushed the neighboring `0x099B00+` command region one step farther too. The old single trailing
  record at `0x09A348-0x09F776` is no longer owned as one source line in `src/bank080000_z80_resources.asm`:
  it now breaks at later proven record starts into three ROM-order include windows,
  `0x09A348-0x09C007`, `0x09C008-0x09E027`, and `0x09E028-0x09F776`.
- Kept that new split structural and conservative. The immediate proof is only record cadence:
  there are 486 local `0xFF` record ends across the old trailing body, the new window starts are all
  real post-terminator starts, and several short repeated 24-byte runs stand out at `0x09AB2A`,
  `0x09E6F7`, and `0x09F4D5`. The exact command format and subsystem owner still need the 68k-side
  consumer.
- Extended the neighboring `0x0A10AB+` five-byte tuple family one run farther forward too. The long
  `0x0A1411-0x0A1D3E` span is no longer a single run-sized `incbin`: it now lives in
  `src/bank080000_a1411.asm` as 215 explicit ROM-order record slices.
- The evidence remains byte-local and conservative: each new slice ends at a proven local `$FF`, each
  pre-terminator payload still divides cleanly into 5-byte units, and the whole run covers
  `0x0A1411-0x0A1D3E` without any new anomaly gaps. That makes the remaining unresolved parts of
  `0x0A10AB-0x0A34FF` narrower and easier to reason about without forcing a subsystem label.
- A final follow-up finished the remaining three regular runs in that same family. The new
  `src/bank080000_a27cb.asm`, `src/bank080000_a2d9b.asm`, and `src/bank080000_a304b.asm` modules now own
  `0x0A27CB-0x0A2D85`, `0x0A2D9B-0x0A303F`, and `0x0A304B-0x0A34FF` as explicit ROM-order record slices
  instead of leaving those spans grouped at run granularity in `src/bank080000_a10ab.asm`.
- That completes the structural split of the whole regular `0x0A10AB-0x0A34FF` five-byte tuple family.
  The local byte proof is unchanged but now fully applied: every record ends at a proven `$FF`, every
  pre-terminator payload length is still a multiple of five bytes, and only the short anomaly gaps
  between runs remain as mixed payload. The last three runs also sharpen the size inventory with
  histograms `6/11/16/21/31/36/41/46/56/76/81/86/91`, `6/11/21/46`, and `6/11/16/21/41`.
