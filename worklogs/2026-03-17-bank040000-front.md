# 2026-03-17 bank040000 front-table pass

- Split the first structurally strong post-fill island in bank `0x040000` out of the coarse
  `0x041000-0x07FFFF` owner into `src/bank040000_tables.asm`.
- Confirmed this span is not just another opaque late-bank blob: `0x041000-0x041BFF` is a regular
  four-byte record cluster with a repeated `[tag][24-bit ROM address]` shape.
- The front `0x041000-0x0418AB` run now stands explicitly as a large local-target table. Every live
  target in that run lands inside `0x041C00-0x07FF66`, which is strong enough to justify a source-owned
  table boundary before the downstream payload format is known.
- The next run at `0x0418AC-0x041B3F` keeps the same four-byte shape but switches cleanly to cross-bank
  targets in `0x0801CD-0x093FD0`, so the phase change is now visible in source instead of being buried
  in one monolithic late-bank `incbin`.
- The short tail at `0x041B80-0x041BBF` also follows the same pattern and points only into
  `0x09414A-0x0961D7`, so it now stands as its own 16-entry table between two explicit `0xFF` fill runs
  at `0x041B40-0x041B7F` and `0x041BC0-0x041BFF`.
- Kept the labels structural. The high byte is mostly `$02` with smaller `$00`, `$01`, and `$FF`
  sentinel cases, so the safest current interpretation is a flagged ROM-reference table rather than a
  finalized pointer table, map list, or script index until the 68k-side consumer is recovered.
- Extended that same evidence one step downstream into the local-target payload body. The first twenty-
  two monotone local targets from the front table now split `0x041C00-0x043FFF` into explicit ROM-order
  record slices in `src/bank040000_payload_front.asm` instead of leaving the whole `0x041C00+` span as
  one anonymous incbin.
- The new split is still intentionally structural. The table proves these starts and their coverage of
  the first `0x2400` bytes of the local-target region, but it still does not identify the consuming
  loader or the record format well enough to call them maps, scripts, graphics metadata, or another
  subsystem.
- A follow-up pass extended that same explicit payload window from `0x043FFF` out to `0x04A70E`.
  Fifty-nine more ROM-order record starts are now source-visible because the local flagged-reference
  table continues monotonically through `0x04A4B0` before the next unsplit byte at `0x04A70F`.
- Kept the naming just as conservative on the extended run. The new evidence is still table structure,
  not subsystem meaning: the starts are proven, ordered, and gap-free, but the record family is not yet
  tied to a recovered 68k-side loader.
- A follow-up pass pushed that same local-target split farther from `0x04A70F` to `0x04E5A9`.
  Twenty-three more table-targeted starts now live in `src/bank040000_payload_mid.asm`, so the bank no
  longer falls back to one monolithic remainder immediately after `0x04A70E`.
- The new tail is especially useful because it sharpens a visible subpattern without overclaiming a
  subsystem: after fifteen variable-length records, the local target table reaches eight consecutive
  `0x4C0`-byte slices from `0x04BFAA` through `0x04E5A9`. That fixed stride is strong structural evidence
  for a repeated payload family even though the loader and exact resource type are still unknown.
- A further follow-up pass extended that same explicit local-target payload window from `0x04E5AA` to
  `0x05FC2F` in `src/bank040000_payload_late.asm`, promoting 160 more monotone same-bank starts out of
  the remaining coarse late-bank blob.
- The strongest new clue in this added span is another repeated fixed-stride family. After an irregular
  lead-in through `0x05D47B`, the flagged-reference table reaches eight consecutive `0x4C0`-byte slices
  at `0x05D630`, `0x05DAF0`, `0x05DFB0`, `0x05E470`, `0x05E930`, `0x05EDF0`, `0x05F2B0`, and
  `0x05F770`; there is also one earlier isolated `0x4C0` record at `0x05C5DA`.
- This keeps the labels structural rather than subsystem-specific, but it materially improves the human
  readability of bank `0x040000`: the next coarse remainder now starts later at `0x05FC30`, and the
  repeated late-bank record cadence is visible directly in source instead of hidden behind one huge
  `incbin`.
- A final same-bank follow-up pass extended that explicit payload window again from `0x05FC30` all the
  way to `0x07FF66` in `src/bank040000_payload_tail.asm`.
- That new module promotes 284 more monotone same-bank starts out of the last coarse late-bank remainder,
  so the front flagged-reference table at `0x041000-0x0418AB` now exposes almost its entire local target
  domain directly in source instead of stopping at `0x05FC2F`.
- The newly exposed tail adds one more repeated-size clue without forcing a subsystem label: two
  consecutive records at `0x06B192` and `0x06B652` also keep the same `0x4C0` stride already seen in the
  earlier eight-record runs.
- The bank now ends with only a tiny residual `incbin` at `0x07FF67-0x07FFFF`, which is a much better
  human-readable stopping point than leaving `0x05FC30-0x07FFFF` as one monolithic late-bank blob.
