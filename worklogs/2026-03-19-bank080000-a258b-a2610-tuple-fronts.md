# 2026-03-19 - bank080000 tuple fronts at 0x0A258B and 0x0A2610

- Replaced the remaining record-local `incbin` slices at `0x0A258B-0x0A2605` and the front of
  `0x0A2610-0x0A2764` through `0x0A275F` with explicit `dc.b` tuples.
- The bytes continue the already-proven FF-terminated five-byte tuple family, so no semantic
  renaming was forced.
- The strongest local structure in the new source is:
  - `0x0A258B-0x0A25FF`: recurring `$43`-page tuples, including short `$9C/$9D/$DE/$DF` pockets
    and matching `$C0/$C4/$CC/$D2/$D8/$DC` tuples.
  - `0x0A2610-0x0A272F`: short paired-row tuples with mirrored `0x08`/`0x00` and later
    `0x08`/`0x28` variants.
  - `0x0A2730-0x0A275F`: a compact late cluster using the same tuple framing but switching to
    `$60/$61` page bytes.
- Verification after the change:
  - `cmd /c verify.bat`
  - `node tools/validate_todos.js`
  - `node tools/run_checks.js`
