# 2026-03-17 bank020000 text pass

- Split `0x021B80-0x02245F` out of `data/rom/bank_020000_03ffff.bin` into
  `src/bank020000_text.asm`.
- Confirmed the front half of the slice is not just isolated hardcoded strings: it is a structured
  text-bearing region with shop, inn/save, and Ocarina-related records interleaved with still-
  undecoded control/script bytes.
- The Ocarina-adjacent text now proves three door-hint strings plus a separate Pyramid transition
  hint: `Jump from the top of the Pyramid and enter a new world.`
- Confirmed `0x022026-0x02245F` is a null-terminated dialogue dictionary word table rather than a
  random prose blob. The entries mix proper nouns (`Alsedo`, `Purapril`, `Sphinx`, `Poseidon`) and
  sentence glue (`remember`, `question`, `storekeeper`, `underwater`), which is strong evidence for
  table-driven dialogue assembly elsewhere in bank `0x020000` or adjacent code.
- Kept `0x021FCA-0x021FDC` conservatively named as Ocarina-adjacent auxiliary data; it sits between
  the door-hint strings and the Pyramid hint but its exact format still needs control-flow proof.
- Split the adjacent upstream quiz/trivia slice `0x021360-0x02171F` out of
  `data/rom/bank_020000_03ffff.bin` into `src/bank020000_quiz.asm`.
- Confirmed this upstream slice is not generic text padding: it contains the tail of a larger word-
  offset table, prompt/control records, two `YES  NO` strings, a `GOLD` label, and ten plain-ASCII
  multiple-choice question blocks with three answers each.
- The question text now gives strong bank-local evidence for a structured quiz system: first magic
  acquired, elf-queen name, first battled monster, `Gragg & Glagg` reward, Wanderer weapon-shop
  inventory, dwarf-village name, Pyramid key, Charmstone price, desert-traversal boots, and a
  non-weapon-selling store (`Gooningle` / `Bacchus` / `Felissimo`).
- Kept `0x021360-0x0213AE` as a raw offset-table tail and `0x0213BD-0x021428` as literal control
  data because the consuming quiz driver and the encoded host/control bytes are still not proven
  from code flow.
- Split the next bank-local structured island at `0x022460-0x022C5F` out of the remaining
  `src/bank020000.asm` tail into `src/bank020000_tables.asm`.
- Confirmed this post-dictionary slice is not more plain prose: `0x022460-0x0224BF` is six 0x10-byte
  same-bank reference descriptors, `0x0224C0-0x0225D7` is a monotone offset table with a clean
  `0x0000` terminator and a plausible payload base at `0x0225D8`, and `0x02292C-0x022AEB` is a stable
  repeated 0x10-byte descriptor band.
- Kept the labels structural rather than naming the region as maps, scripts, or object lists. The
  byte patterns strongly justify a ROM-order table split, but the code that consumes these offsets and
  records still needs to be found before stronger semantics are safe.
- Split the front bank-local index island at `0x0211CA-0x02135F` out of the remaining coarse
  `0x020000-0x02135F` owner into `src/bank020000_offsets.asm`.
- Confirmed this front slice is structured linkage data rather than more anonymous mixed text:
  `0x0211CA-0x0211D9` is a four-entry absolute pointer list that targets the local offset data,
  `0x021720`, `0x021A06`, and `0x021FCA`; `0x0211DA-0x021257` is a 52-entry bank-relative offset table
  with zero sentinels; and `0x021258-0x02135F` is a second bank-relative table whose words increase
  monotonically after a leading zero sentinel.
- Kept the new labels structural because the bytes prove table ownership and target ranges, but the
  actual 68k-side text/script/menu driver that walks those tables is still not identified.
- Split another later bank-local structured island at `0x023AB6-0x024537` out of the remaining
  `src/bank020000.asm` tail into `src/bank020000_records.asm`.
- Confirmed this later slice is not plain text or random packed bytes: it is a long band of big-endian
  word records separated by repeated `$FFFF` sentinels, with several recurring record shapes visible
  across the band.
- Kept the labels structural rather than forcing a subsystem name. The delimiter pattern proves stable
  ROM-order record boundaries, but the consuming code and exact field meanings still need to be proven
  before these records can safely be called maps, placement data, scripts, or menu layouts.
- Split the middle payload of the earlier `0x022460-0x022C5F` structured island at
  `0x022662-0x02292B` out of its remaining `incbin` in `src/bank020000_tables.asm`.
- Confirmed the front offset table at `0x0224C0` does more than point into one anonymous payload blob:
  it resolves to 139 unique starts in this span, with one duplicate offset at `$0260`, one `0x8A`-byte
  lead-in record, eighty-five 6-byte records, eight 8-byte records, and a final terminator word before
  the later `0x02292C` descriptor band begins.
- Kept those new labels structural rather than naming the three-word records prematurely. The table now
  proves ROM-order record boundaries and small record families, but the actual consumer and field
  meanings still need control-flow evidence before stronger subsystem names are safe.
- Extended the same post-dictionary structured island farther through `0x022DB7` instead of dropping back
  to a coarse bank-local `incbin` at `0x022C60`.
- Confirmed `0x022C56-0x022D3F` continues the preceding tail's record shape: it is another ROM-order run
  of variable-length word records terminated by `$FFFF`, including one longer mixed record at `0x022C56`,
  several short six-word records, and another longer record at `0x022CFC`.
- Confirmed the next `0x022D40-0x022D6B` span is not random residue either: it is a repeated 22-word
  `$0BFF` band, followed by compact `$FF00`-terminated word records at `0x022D6C-0x022DB7`, with `$FE00`
  markers embedded in the two longer compact records.
- Kept those new labels structural. The byte patterns now justify smaller ROM-order slices and explicit
  delimiter families, but they still do not prove whether this tail belongs to a map/object/script/menu
  subsystem without the code that consumes the `$FFFF`, `$FE00`, `$FF00`, and repeated `$0BFF` markers.
- Split the bridge region immediately after that first post-dictionary island one step farther at
  `0x022DB8-0x023AB5` instead of leaving the whole gap as one bank-local `incbin` in `src/bank020000.asm`.
- Confirmed the front of the later tail is structurally adjacent to the earlier compact-record family:
  after a still-opaque recurring-word band at `0x022DB8-0x0237B3`, the bytes expose a standalone `$FFFF`
  word at `0x0237B4`, two more explicit `$FF00`-terminated compact word records at `0x0237B6-0x0237CD`
  and `0x0237CE-0x0237DB`, a short three-word lead-in at
  `0x0237DC-0x0237E1`, and another standalone `$FFFF` at `0x0237E2` before the remaining mixed payload at
  `0x0237E4-0x023AB5` resumes.
- Kept those bridge labels structural rather than pretending the larger recurring-word bands are decoded.
  The local sentinel words are strong enough to expose real ROM-order boundaries and to tie the bridge more
  tightly to the neighboring compact-record / sentinel-record families, but the consuming code and exact
  field semantics for `0x022DB8-0x023AB5` are still open.
