# 2026-03-19 - bank080000 tuple runs at 0x0A2400 and 0x0A27CB

- Replaced the remaining record-local `incbin` slices in `src/bank080000_a2400.asm` and
  `src/bank080000_a27cb.asm` with explicit `dc.b` tuples.
- Both windows continue the already-proven FF-terminated five-byte tuple family, so the names
  stay structural instead of forcing a loader or subsystem guess.
- The strongest local structure now visible in source is:
  - `0x0A2400-0x0A248F`: repeated paired `0x60/0x68` and `0x40/0x48` variants around pages
    `0x18`, `0x2E`, `0x37`, `0x42`, `0x44`, `0x49`, and `0x4B`.
  - `0x0A249A-0x0A252B`: a compact `0x01`-page cluster around `0xE0-0xFE` with mirrored
    `0x08/0x09` front bytes and short singleton records at `0x0A2515` and `0x0A251B`.
  - `0x0A252C-0x0A2585`: a late `0x60/0x61` ladder that walks through `0x80/0x90/0x9C/0xB0`
    and then into the `0xBC/0xCC/0xDC/0xEC` and `0xFC/0x0C/0x1C/0x2C/0x3C/0x4C/0x5C/0x6C`
    clusters before the already explicit bridge at `0x0A2586`.
  - `0x0A27CB-0x0A2877`: a short `0x61`-page staircase followed by compact `0x20`, `0x00`,
    `0x10`, and `0x18` page pockets around `0x48/0x4B/0x5A/0x5B/0x64/0x70/0x8E/0x94/0x9A`.
  - `0x0A28C8-0x0A2C74`: larger composite records dominated by `0x68/0x69` pages with recurring
    `0x4A/0x3A/0x2E/0x20/0x18/0x0B`, `0x63`, `0x8E`, and `0x5E` clusters.
  - `0x0A2C75-0x0A2D85`: a late `0x61` pocket followed by another `0x60/0x61` ladder that leads
    directly into the already explicit embedded-`$FF` anomaly record at `0x0A2D86`.
- Verification after the change:
  - `cmd /c verify.bat`
  - `node tools/validate_todos.js`
  - `node tools/run_checks.js`
