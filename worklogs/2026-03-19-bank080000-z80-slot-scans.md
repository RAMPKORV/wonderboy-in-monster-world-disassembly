# 2026-03-19 bank080000 Z80 slot scans

- Tightened the next proven Z80 main-body frontier in `src/bank080000_mid_z80_program.asm` from `0x0982BA` through `0x098392`.
- The former opaque `$FC`, `$FD`, and `$FF` meta-command targets are now source-visible as structural slot-management helpers instead of raw `incbin` slices.
- `0x0982BA-0x0982E1` walks ten `0x20`-byte records rooted at `0x1EC0`, counts through them with `0x1C07`, conditionally calls `0x02FA` for active entries whose byte at `+0x10` also has bit 7 set, and then sets bit 0 in `0x1C03`.
- `0x0982E2` only clears bit 0 in `0x1C03`, and `0x0982E8-0x0982F9` is now explicit as a reusable helper that packs the current `IX` pointer into a slot index relative to the `0x1EC0` base.
- The neighboring `0x0982FA-0x098320`, `0x098321-0x098344`, and `0x098345-0x098368` helpers are now source-visible too: they scan the `0x1C80` and `0x1EC0` slot tables, compare against either the packed slot index or the selected byte at `0x1C28`, and call the shared `0x02FA` / `0x0446` helpers before clearing matched active slots.
- `0x098369-0x098392` is now explicit as the `$FF` meta-command target that retires every active `0x1EC0`-rooted slot and clears the nearby state bytes/words at `0x1C2A`, `0x1C09`, and `0x1C0B`.
- The unresolved Z80 main-body continuation now starts at `0x098393`, which is a better next frontier than the older `0x0982BA` break because the intervening control-flow-owned slot logic is no longer hidden.
