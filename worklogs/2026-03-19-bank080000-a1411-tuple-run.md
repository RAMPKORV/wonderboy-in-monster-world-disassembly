# 2026-03-19 - bank080000 tuple run at `0x0A1411-0x0A1D3E`

- Replaced the former coarse run-sized `incbin` owner in `src/bank080000_a1411.asm` with
  explicit `dc.b` records.
- This window still fits the already-proven FF-terminated five-byte tuple family, so the
  names stay structural instead of forcing a loader or subsystem guess.
- The strongest local structure now visible in source is:
  - early mirrored `0x18/0x10/0x08/0x00` quadrant variants through the front record group.
  - a broader middle ladder around `0x0A15BF-0x0A1B20` that repeatedly reuses `0xC0`-through-`0xC5`
    tuple pages with paired placements.
  - later compact `0x60/0x68` and `0x40/0x48` pockets immediately before the mixed bridge at
    `0x0A1D3F`.
- That removes another 215 record-local opaque slices from the same tuple family. The
  remaining uncertainty in this subsection is record meaning and loader ownership, not local
  byte coverage.
- Verification after the change:
  - `cmd /c verify.bat`
  - `node tools/validate_todos.js`
  - `node tools/run_checks.js`
