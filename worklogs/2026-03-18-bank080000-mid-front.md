# 2026-03-18 bank080000 mid front

- Split the former single `0x098000-0x09991F` front slice in `src/bank080000_mid.asm` at
  `0x099200`, leaving `0x098000-0x0991FF` under the existing strongly Z80-like opcode-dense
  label and moving `0x099200-0x09991F` into the new ROM-order module
  `src/bank080000_mid_front.asm`.
- Promoted `0x099200-0x09927F` into two explicit monotone byte bands instead of leaving the
  whole pre-descriptor tail opaque.
- Promoted `0x099280-0x099648` into 51 fixed 19-byte records because the entire span stays on a
  stable 19-byte cadence and every aligned record uses the same third/fourth-byte control family
  (`F0C0`, `D0C0`, or `B0C0`).
- Kept `0x099649-0x09988F` conservative as two smaller mixed/control tails. The bytes are clearly
  structured, but they do not yet justify one stronger tuple or pointer format without the loader.
- Promoted the final `0x099890-0x09991F` trailer into source as a short local-offset word band,
  a packed byte band, eight repeated `0x01F1` words, a 14-byte trailer, and a final word tail
  that leads directly into the already split descriptor/layout records at `0x099920`.
- Kept the names structural and recorded the uncertainty in `docs/open_questions.md` rather than
  forcing an audio, credits, script, or other subsystem label before code-flow evidence exists.
