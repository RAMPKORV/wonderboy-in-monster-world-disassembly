# 2026-03-19 - bank080000 tuple run at 0x0A1D4F-0x0A23F5

- Replaced the remaining record-local `incbin` bodies in `src/bank080000_a1d4f.asm` with
  explicit `dc.b` records.
- This window still fits the already-proven FF-terminated five-byte tuple family, so the
  names stay structural instead of forcing a loader or subsystem guess.
- The strongest local structure now visible in source is:
  - `0x0A1D4F-0x0A1D50`: two singleton `$FF` records before the usual tuple cadence resumes.
  - `0x0A1D89-0x0A1DDB`: three mirrored `0x42`-page 41-byte runs.
  - `0x0A1FB3-0x0A2194`: a broader `0x60/0x61` ladder with repeated `0x00/e0` position pairs.
  - `0x0A21B3-0x0A22C9`: compact low-page pockets around `0x20`, `0x00`, `0x08`, and `0x60`.
  - `0x0A2337-0x0A23F5`: a late mixed tail that reintroduces `0x68`, `0x60`, and `0x40` page
    variants immediately before the next explicit run at `0x0A2400`.
- That removes another 113 record-local opaque slices from the same tuple family. The
  remaining uncertainty in this subsection is record meaning and loader ownership, not local
  byte coverage.
- Verification after the change:
  - `cmd /c verify.bat`
  - `node tools/validate_todos.js`
  - `node tools/run_checks.js`
