# Data Layout

This directory stores extracted opaque ROM blobs that are still preserved byte-for-byte.
`data/rom/wonderboy_000200.bin` currently contains the ROM body from offset `0x000200`
through `0x0BFFFF`.
